params = list(covar = "age_65_plus")
source("./covar_summary.R")

params = list(covar = "college")
source("./covar_summary.R")

params = list(covar = "black")
source("./covar_summary.R")

params = list(covar = "hispanic")
source("./covar_summary.R")
